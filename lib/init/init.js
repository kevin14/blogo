/*
handle init event
to create the slog blog
 */

var async = require('async');
var util = require('../util');
var fs = util.fs;
var path = require('path');

//init slog folders and files
module.exports = function(cwd,args,callback){

	var src = path.join('../../assets')

	fs.copyDir()

}